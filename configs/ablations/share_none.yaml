# Ablation: independent per-stage weights (no weight sharing)
defaults:
  - ../default

run_name: ablate_share_none
model:
  num_stages: 6           # fewer stages to keep param count tractable
  weight_sharing: share_none
